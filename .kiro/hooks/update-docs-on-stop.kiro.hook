{
  "enabled": true,
  "name": "迭代完成后更新文档",
  "description": "在 agent 完成变更迭代后，自动将 docs/architecture.md 更新为反映项目最新状态的文档（不体现历史变更），同时将本次迭代的变更摘要追加到 docs/changelog.md 迭代历史文档中。",
  "version": "1",
  "when": {
    "type": "agentStop"
  },
  "then": {
    "type": "askAgent",
    "prompt": "请完成以下两项文档维护工作：\n\n1. 更新项目文档 `docs/architecture.md`：\n   - 根据当前项目代码的最新状态，更新该文档的所有内容（技术栈、目录结构、数据模型、功能列表等）。\n   - 文档应只反映项目的当前状态，不要包含任何历史变更记录。\n   - 保持文档的现有格式和风格，使用中文撰写。\n\n2. 维护迭代历史文档 `docs/changelog.md`：\n   - 如果该文件不存在，先创建它，标题为「# 迭代历史」。\n   - 在文件顶部（标题之后）追加本次迭代的变更记录，格式如下：\n     ```\n     ## [日期] 迭代摘要标题\n     - 变更点 1\n     - 变更点 2\n     ...\n     ```\n   - 根据本次 agent 执行中实际做出的代码变更来总结，保持简洁准确。\n   - 历史记录按时间倒序排列（最新的在最上面）。\n\n注意：如果本次执行没有对代码做出实质性变更（例如只是回答问题或查看文件），则跳过文档更新，不做任何修改。"
  }
}